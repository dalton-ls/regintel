# Frozen Worker: regintel-admin-proxy

This package is a **410 stub**. The live site and admin API are the
`regintel` Worker at https://regintel.regintel.workers.dev (see
[CONTRIBUTING.md](../../CONTRIBUTING.md)).

Do not deploy the static site under this Worker name. Occupying the old
hostname prevents a name collision with the live project.
